import {TriggerTest} from '../../../lib/index.ts'

// deno-lint-ignore require-await
TriggerTest.prototype.register = async function(eventKey: string) {
    // TODO: Register this with modules or whatever
}