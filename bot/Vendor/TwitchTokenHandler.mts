export default class TwitchTokenHandler {
    /*
    TODO
     This does all stuff that happens in the background.
     - token exchange
     - refresh
     Supply valid client ID, client secret and redirect URI.
     After the first authentication, load user ID and previous tokens from the database.


    */
}